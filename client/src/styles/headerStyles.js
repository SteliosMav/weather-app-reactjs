import { AppBar } from '@material-ui/core';
import styled from 'styled-components';

export const HeaderAppBar = styled(AppBar)`
  background-image: linear-gradient(#324a7d, #475e8f);
  width: 100vw;
`;
